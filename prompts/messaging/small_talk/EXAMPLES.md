# Example

Input:

```json
{
  "state": "CV_PENDING",
  "user_message": "how are you?"
}
```

Output:

```json
{
  "message": "Doing well. Please send your CV as text, a file, or a short voice description so I can continue."
}
```
