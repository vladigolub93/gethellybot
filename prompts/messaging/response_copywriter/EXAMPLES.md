# Example

Intent:

```json
{
  "meaning": "Ask the user to send salary, location, and work format."
}
```

Output:

```json
{
  "message": "Please send your salary expectations, current location, and preferred work format: remote, hybrid, or office."
}
```
