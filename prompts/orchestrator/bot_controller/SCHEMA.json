{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BotControllerDecision",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "intent": {
      "type": "string",
      "enum": [
        "on_flow_input",
        "small_talk",
        "support_request",
        "clarification_request",
        "off_topic",
        "destructive_intent",
        "unknown"
      ]
    },
    "tone": {
      "type": "string",
      "enum": ["neutral", "friendly", "supportive", "firm"]
    },
    "response_mode": {
      "type": "string",
      "enum": ["answer", "recover", "clarify", "redirect"]
    },
    "keep_current_state": {
      "type": "boolean"
    },
    "proposed_action": {
      "type": ["string", "null"],
      "maxLength": 120
    },
    "response_text": {
      "type": ["string", "null"],
      "maxLength": 500
    },
    "reason_code": {
      "type": ["string", "null"],
      "enum": [
        "current_step_guidance",
        "unsupported_input",
        "small_talk_redirect",
        "help",
        "deletion_confirmation_needed",
        "ambiguous_message",
        null
      ]
    }
  },
  "required": [
    "intent",
    "tone",
    "response_mode",
    "keep_current_state",
    "proposed_action",
    "response_text",
    "reason_code"
  ]
}
